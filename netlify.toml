[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# Indirizzo del backend pubblicato su Render
[build.environment]
  VITE_API_BASE_URL = "https://miktattoo-backend.onrender.com/api"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
